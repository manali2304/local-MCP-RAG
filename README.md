# local-MCP-RAG
The MCP tool can then be used inside VS Code / MCP-enabled clients to search local internal documentation using natural language.
- Node.js MCP server
- ChromaDB vector database
- Sentence Transformers embeddings (`all-MiniLM-L6-v2`)
- Python embedding worker
- Docling document parser
- Incremental indexing
- Semantic search
